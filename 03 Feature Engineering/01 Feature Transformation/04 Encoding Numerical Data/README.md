# Encoding Numerical Features

1. **Discretization (Binning)** → Converts continuous numerical values into **categorical bins/intervals**. 

2. **Binarization** → Converts numerical values into **binary (0/1) features** based on a threshold.
